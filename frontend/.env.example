# Deployed ClauseLens contract on GenLayer Bradbury Testnet.
# Optional — the app falls back to the hardcoded address if this is not set.
VITE_CONTRACT_ADDRESS=0x2F032bf686F346a9E86DAE9B5762Bf2B17b7cFF8

# Optional. Used in the share-on-X URL. Falls back to window.location.origin at runtime.
# VITE_APP_URL=https://clauselens.vercel.app
